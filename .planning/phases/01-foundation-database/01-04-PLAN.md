---
phase: 01-foundation-database
plan: 01-04
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - apps/api/src/app.ts
  - apps/api/src/middleware/error.ts
  - apps/api/src/middleware/validate.ts
  - apps/api/src/shared/apiResponse.ts
  - apps/api/src/shared/asyncRoute.ts
  - apps/api/src/modules/health/health.routes.ts
  - apps/api/src/modules/auth/auth.routes.ts
  - apps/api/src/modules/users/users.routes.ts
  - apps/api/src/modules/trips/trips.routes.ts
  - apps/api/src/modules/destinations/destinations.routes.ts
  - apps/api/src/modules/activities/activities.routes.ts
  - apps/api/src/modules/itinerary/itinerary.routes.ts
  - apps/api/src/modules/budget/budget.routes.ts
  - apps/api/src/modules/packing/packing.routes.ts
  - apps/api/src/modules/sharing/sharing.routes.ts
  - apps/api/src/modules/notes/notes.routes.ts
autonomous: true
requirements:
  - FOUND-03
  - FOUND-04
user_setup: []
must_haves:
  truths:
    - D-12 D-13 D-14 D-15 are implemented as reusable API route, validation, async, and error-handling conventions.
    - The API exposes clear module placeholders for core Traveloop domains without implementing full later-phase behavior.
    - Validation errors return a stable `VALIDATION_ERROR` code and structured `details`.
  artifacts:
    - `apps/api/src/middleware/validate.ts` validates Zod schemas.
    - `apps/api/src/middleware/error.ts` emits `{ error: { code, message, details } }`.
    - Domain route files exist for auth, users, trips, destinations, activities, itinerary, budget, packing, sharing, and notes.
  key_links:
    - `apps/api/src/app.ts` mounts every domain route under `/api/*`.
    - Route placeholders use `success()` or `notImplemented()` helpers instead of ad hoc JSON shapes.
---

<objective>
Establish backend API conventions and module boundaries.

Purpose: Let later feature phases add behavior inside stable Express, Zod, Prisma, and error-response patterns.
Output: API middleware, shared response helpers, and route modules for every core domain.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-database/01-CONTEXT.md
@.planning/phases/01-foundation-database/01-RESEARCH.md
@apps/api/src/app.ts
@apps/api/src/modules/health/health.routes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add structured response, validation, async, and error middleware</name>
  <files>apps/api/src/middleware/error.ts, apps/api/src/middleware/validate.ts, apps/api/src/shared/apiResponse.ts, apps/api/src/shared/asyncRoute.ts, apps/api/src/app.ts</files>
  <read_first>apps/api/src/app.ts, apps/api/src/config/env.ts, .planning/phases/01-foundation-database/01-CONTEXT.md</read_first>
  <action>Create `success(data)` returning `{ data }`, `apiError(code, message, details?)`, and `notImplemented(domain)` helpers in `apiResponse.ts`. Create `asyncRoute(fn)` that forwards rejected promises to Express `next`. Create `validateBody(schema)` in `validate.ts` using Zod; invalid bodies must call `next(apiError("VALIDATION_ERROR", "Request validation failed", issueDetails))`. Create `errorMiddleware` in `error.ts` that returns status 400 for `VALIDATION_ERROR`, 404 for `NOT_FOUND`, 501 for `NOT_IMPLEMENTED`, and 500 with code `INTERNAL_SERVER_ERROR` otherwise. Update `app.ts` to use the error middleware after routes and keep successful responses shaped as `{ data: ... }`.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - `apps/api/src/middleware/validate.ts` contains `VALIDATION_ERROR`.
    - `apps/api/src/middleware/error.ts` contains `INTERNAL_SERVER_ERROR` and `NOT_IMPLEMENTED`.
    - `apps/api/src/shared/apiResponse.ts` contains `success` and `apiError`.
    - `apps/api/src/app.ts` imports `errorMiddleware`.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>All API modules can share one response and validation style.</done>
</task>

<task type="auto">
  <name>Task 2: Create domain route modules for later phases</name>
  <files>apps/api/src/modules/auth/auth.routes.ts, apps/api/src/modules/users/users.routes.ts, apps/api/src/modules/trips/trips.routes.ts, apps/api/src/modules/destinations/destinations.routes.ts, apps/api/src/modules/activities/activities.routes.ts, apps/api/src/modules/itinerary/itinerary.routes.ts, apps/api/src/modules/budget/budget.routes.ts, apps/api/src/modules/packing/packing.routes.ts, apps/api/src/modules/sharing/sharing.routes.ts, apps/api/src/modules/notes/notes.routes.ts, apps/api/src/app.ts, apps/api/src/modules/health/health.routes.ts</files>
  <read_first>apps/api/src/app.ts, apps/api/src/shared/apiResponse.ts, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</read_first>
  <action>Create route modules exporting Express routers for `auth`, `users`, `trips`, `destinations`, `activities`, `itinerary`, `budget`, `packing`, `sharing`, and `notes`. Each module must expose `GET /` returning `notImplemented("<domain>")` through the shared helper with code `NOT_IMPLEMENTED`; do not implement full feature behavior. Update `app.ts` to mount them exactly at `/api/auth`, `/api/users`, `/api/trips`, `/api/destinations`, `/api/activities`, `/api/itinerary`, `/api/budget`, `/api/packing`, `/api/sharing`, and `/api/notes`. Update health routes to add `GET /api/status/seed` using `getSeedHealth()` from plan 03 if that file exists, or leave a typed import-ready placeholder that returns `NOT_IMPLEMENTED` until plan 03 completes.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - `apps/api/src/app.ts` contains `/api/auth`, `/api/trips`, `/api/itinerary`, `/api/budget`, `/api/packing`, `/api/sharing`, and `/api/notes`.
    - Each domain route file contains `Router()`.
    - Each placeholder route returns `NOT_IMPLEMENTED`.
    - No route file contains full trip CRUD, auth session, budget mutation, or sharing behavior.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Backend module boundaries are ready without stealing scope from later feature phases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Client input to API | JSON bodies enter Express routes. |
| Domain placeholder to later implementation | Later phases will replace placeholders with authenticated behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05 | Tampering | request validation | mitigate | Centralize Zod body validation and structured validation errors. |
| T-04 | Elevation of Privilege | private route placeholders | mitigate | Do not fake ownership logic; leave feature behavior for auth/authorization phases. |
| T-01-04 | Information Disclosure | server errors | mitigate | Generic 500 responses should not leak stack traces in JSON. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run build`
- [ ] `rg -n "VALIDATION_ERROR|INTERNAL_SERVER_ERROR|NOT_IMPLEMENTED" apps/api/src`
- [ ] `rg -n "/api/auth|/api/trips|/api/itinerary|/api/budget|/api/packing|/api/sharing|/api/notes" apps/api/src/app.ts`
</verification>

<success_criteria>
- FOUND-03 and FOUND-04 have concrete backend conventions.
- Later phases can implement feature APIs by replacing placeholders, not inventing new response/validation patterns.
- API errors are consistent and safe by default.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-database/01-04-SUMMARY.md`.
</output>
