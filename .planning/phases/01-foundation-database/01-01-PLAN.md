---
phase: 01-foundation-database
plan: 01-01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.base.json
  - .env.example
  - docker-compose.yml
  - prisma/schema.prisma
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/api/src/app.ts
  - apps/api/src/server.ts
  - apps/api/src/config/env.ts
  - apps/api/src/db/prisma.ts
  - apps/api/src/modules/health/health.routes.ts
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/index.html
  - apps/web/src/main.tsx
  - apps/web/src/App.tsx
  - apps/web/src/app/FoundationProbe.tsx
  - apps/web/src/styles/global.css
  - packages/shared/package.json
  - packages/shared/src/index.ts
autonomous: true
requirements:
  - FOUND-01
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - FOUND-06
user_setup:
  - Run `docker compose up -d db` before Prisma migration commands if PostgreSQL is not already running locally.
must_haves:
  truths:
    - D-01 D-02 D-03 D-04 are visible in a TypeScript workspace with `apps/web`, `apps/api`, `packages/shared`, and no forbidden BaaS dependencies.
    - A real frontend interaction can call the Express API and trigger a Prisma write/read against local PostgreSQL.
    - D-12 D-13 D-14 are started through modular health routes, environment validation, and structured API responses.
  artifacts:
    - `package.json` declares workspaces for `apps/*` and `packages/*`.
    - `docker-compose.yml` declares a local PostgreSQL service named `db`.
    - `prisma/schema.prisma` contains datasource provider `postgresql` and model `FoundationProbe`.
    - `apps/api/src/modules/health/health.routes.ts` exposes foundation probe read/write endpoints.
    - `apps/web/src/app/FoundationProbe.tsx` renders a button that calls the probe endpoint.
  key_links:
    - `apps/api/src/db/prisma.ts` exports the Prisma client consumed by `health.routes.ts`.
    - `apps/web/src/app/FoundationProbe.tsx` calls the API URL configured for the Vite app.
---

<objective>
Create the Walking Skeleton foundation for Traveloop.

Purpose: Prove the chosen stack works end-to-end before expanding the domain schema.
Output: npm workspace scaffold, local PostgreSQL config, minimal Prisma probe model, Express API, Vite frontend, shared package, and one UI-driven API/database probe.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-database/01-CONTEXT.md
@.planning/phases/01-foundation-database/01-RESEARCH.md
@.planning/phases/01-foundation-database/01-UI-SPEC.md
@.planning/phases/01-foundation-database/01-PATTERNS.md
@.planning/phases/01-foundation-database/01-SKELETON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the workspace, local database config, and shared TypeScript baseline</name>
  <files>package.json, package-lock.json, tsconfig.base.json, .env.example, docker-compose.yml, apps/api/package.json, apps/api/tsconfig.json, apps/web/package.json, apps/web/tsconfig.json, packages/shared/package.json, packages/shared/src/index.ts</files>
  <read_first>.planning/phases/01-foundation-database/01-CONTEXT.md, .planning/phases/01-foundation-database/01-RESEARCH.md, .planning/research/STACK.md, .planning/phases/01-foundation-database/01-SKELETON.md</read_first>
  <action>Create a root npm workspace with `"workspaces": ["apps/*", "packages/*"]`. Add root scripts exactly: `"dev": "npm run dev --workspace @traveloop/api & npm run dev --workspace @traveloop/web"`, `"build": "npm run build --workspaces --if-present"`, `"typecheck": "npm run typecheck --workspaces --if-present"`, `"db:validate": "prisma validate"`, `"db:migrate": "prisma migrate dev --name init"`, `"db:seed": "tsx prisma/seed.ts"`, and `"verify": "npm run build && npm run db:validate"`. Install root dev dependencies `typescript`, `tsx`, `prisma`, and `@types/node`; API dependencies `@prisma/client`, `cors`, `dotenv`, `express`, `zod`; API dev dependencies `@types/cors`, `@types/express`, `tsx`; web dependencies `@vitejs/plugin-react`, `vite`, `react`, `react-dom`, and `lucide-react`; shared dependencies `zod`. Create `docker-compose.yml` with service `db`, image `postgres:16-alpine`, database `traveloop_dev`, user `traveloop`, password `traveloop`, and port mapping `5432:5432`. Create `.env.example` containing `DATABASE_URL=postgresql://traveloop:traveloop@localhost:5432/traveloop_dev?schema=public`, `API_PORT=4000`, `WEB_ORIGIN=http://localhost:5173`, and `VITE_API_URL=http://localhost:4000`. Do not add Firebase, Supabase, MongoDB, Atlas, or BaaS packages.</action>
  <verify>npm run build --workspaces --if-present</verify>
  <acceptance_criteria>
    - `package.json` contains `"workspaces": [` and scripts `db:migrate`, `db:seed`, and `verify`.
    - `docker-compose.yml` contains `postgres:16-alpine`, `traveloop_dev`, and `5432:5432`.
    - `.env.example` contains `DATABASE_URL=postgresql://traveloop:traveloop@localhost:5432/traveloop_dev?schema=public`.
    - `rg -n "firebase|supabase|mongodb|atlas" package.json apps packages prisma` returns no matches.
  </acceptance_criteria>
  <done>The repository can install as a TypeScript workspace with local PostgreSQL configuration and no forbidden service dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Build the minimal database-backed foundation probe</name>
  <files>prisma/schema.prisma, apps/api/src/app.ts, apps/api/src/server.ts, apps/api/src/config/env.ts, apps/api/src/db/prisma.ts, apps/api/src/modules/health/health.routes.ts, apps/web/index.html, apps/web/src/main.tsx, apps/web/src/App.tsx, apps/web/src/app/FoundationProbe.tsx, apps/web/src/styles/global.css</files>
  <read_first>package.json, .env.example, prisma/schema.prisma, apps/api/src/app.ts, apps/web/src/App.tsx, .planning/phases/01-foundation-database/01-SKELETON.md</read_first>
  <action>Create `prisma/schema.prisma` with `datasource db { provider = "postgresql"; url = env("DATABASE_URL") }`, generator `client`, and model `FoundationProbe` with fields `id String @id @default(cuid())`, `label String`, `createdAt DateTime @default(now())`. Create API env parsing in `apps/api/src/config/env.ts` with Zod requiring `DATABASE_URL`, `API_PORT`, `WEB_ORIGIN`, and `NODE_ENV` defaulting to `development`. Create `apps/api/src/db/prisma.ts` exporting `prisma = new PrismaClient()`. Create Express `app.ts` with JSON middleware, CORS using `env.WEB_ORIGIN`, `/api/health` returning `{ "data": { "status": "ok" } }`, and mounted health routes. Create `health.routes.ts` with `GET /api/foundation-probes/latest` returning latest probe or null and `POST /api/foundation-probes` validating `{ label: string().min(2).max(80) }`, writing a row through Prisma, and returning `{ data: probe }` with status 201. Create Vite React files where `FoundationProbe.tsx` renders a text input defaulting to `Traveloop foundation`, a button labelled `Save foundation probe`, and after click displays the returned label. Use `VITE_API_URL` for fetch calls.</action>
  <verify>npm run db:validate && npm run build</verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` contains `provider = "postgresql"` and `model FoundationProbe`.
    - `apps/api/src/modules/health/health.routes.ts` contains `router.post("/foundation-probes"` and `prisma.foundationProbe.create`.
    - `apps/api/src/app.ts` contains `/api/health` and returns a top-level `data` object.
    - `apps/web/src/app/FoundationProbe.tsx` contains `Save foundation probe` and `import.meta.env.VITE_API_URL`.
    - `npm run db:validate` exits 0.
  </acceptance_criteria>
  <done>A user-visible frontend control can exercise an Express route backed by Prisma/PostgreSQL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser to API | The Vite app sends JSON requests to Express. |
| API to PostgreSQL | Express routes read and write through Prisma using `DATABASE_URL`. |
| npm registry to repo | Workspace dependencies enter through `package-lock.json`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01 | Tampering | stack dependencies | mitigate | Verify no forbidden BaaS dependencies and commit `package-lock.json`. |
| T-05 | Tampering | API input | mitigate | Validate probe input with Zod and return structured validation errors in later API plan. |
| T-01-01 | Information Disclosure | environment config | mitigate | Commit only `.env.example`; never commit real `.env` values. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run db:validate`
- [ ] `npm run build`
- [ ] `rg -n "FoundationProbe|foundationProbe|Save foundation probe" prisma apps`
- [ ] `rg -n "firebase|supabase|mongodb|atlas" package.json apps packages prisma` returns no matches
</verification>

<success_criteria>
- The local stack has a runnable frontend, API, and Prisma baseline.
- One UI action is wired to one API endpoint that performs a real database read/write.
- D-01 through D-04 and the Walking Skeleton contract are visible in source files.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-database/01-01-SUMMARY.md`.
</output>
