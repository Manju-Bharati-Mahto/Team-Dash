---
phase: 01-foundation-database
plan: 01-03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - prisma/seed.ts
  - package.json
  - apps/api/src/db/seedVerification.ts
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
user_setup:
  - Run `docker compose up -d db` before seed verification if PostgreSQL is not already running.
must_haves:
  truths:
    - D-09 D-10 D-11 are satisfied by deterministic local seed data for a realistic multi-city Traveloop demo.
    - Seeded public share data is demo-safe and does not expose production secrets.
    - Seed verification can prove users, trips, cities, activities, expenses, packing items, notes, saved destinations, and shares exist.
  artifacts:
    - `prisma/seed.ts` creates demo data across all required core tables.
    - `apps/api/src/db/seedVerification.ts` exports counts or checks used by health/status routes.
  key_links:
    - `package.json` `db:seed` script runs `tsx prisma/seed.ts`.
    - Seeded trip records reference seeded owner, city, stop, activity, expense, packing, note, and public share records.
---

<objective>
Create deterministic local seed data for the Traveloop demo path.

Purpose: Ensure search, dashboard, budget, packing, notes, and public share slices have realistic relational data available from the start.
Output: Prisma seed script and seed verification helper.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-database/01-CONTEXT.md
@.planning/phases/01-foundation-database/01-RESEARCH.md
@prisma/schema.prisma
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed a realistic multi-city Traveloop dataset</name>
  <files>prisma/seed.ts</files>
  <read_first>prisma/schema.prisma, package.json, .planning/phases/01-foundation-database/01-RESEARCH.md</read_first>
  <action>Create `prisma/seed.ts` using `PrismaClient`. Use deterministic upserts keyed by unique fields. Seed demo user `demo@traveloop.local` with display name `Maya Rao`, profile metadata, and demo-only password placeholder text `phase-2-will-hash-passwords` if the schema includes a password field. Seed at least five cities: Kyoto, Barcelona, Reykjavik, Lisbon, and Singapore with country, region, cost index, popularity score, and image metadata. Seed at least twelve activities distributed across those cities with category, estimated cost, duration minutes, and description. Seed one trip named `Spring Cities Circuit` owned by Maya with at least three ordered stops, planned activities on stops, expenses in categories transport/stay/activities/meals/other, packing items in clothing/documents/electronics/essentials/toiletries, trip notes for trip/stop/day scopes, a public share token `demo-spring-cities`, and at least two saved destinations. End the script with console output exactly `Seed complete: Traveloop demo data ready`.</action>
  <verify>npm run db:seed</verify>
  <acceptance_criteria>
    - `prisma/seed.ts` contains `demo@traveloop.local`.
    - `prisma/seed.ts` contains `Spring Cities Circuit`.
    - `prisma/seed.ts` contains `demo-spring-cities`.
    - `prisma/seed.ts` contains `Seed complete: Traveloop demo data ready`.
    - `npm run db:seed` exits 0 against local PostgreSQL.
  </acceptance_criteria>
  <done>Local seed data can repopulate a rich demo state without external APIs.</done>
</task>

<task type="auto">
  <name>Task 2: Add seed verification helpers for API and checks</name>
  <files>apps/api/src/db/seedVerification.ts</files>
  <read_first>apps/api/src/db/prisma.ts, prisma/schema.prisma, prisma/seed.ts</read_first>
  <action>Create `apps/api/src/db/seedVerification.ts` exporting `getSeedHealth()` that returns counts for users, trips, cities, activities, expenses, packingItems, tripNotes, publicShares, and savedDestinations. The function must use the shared Prisma client from `apps/api/src/db/prisma.ts` and return an object shaped `{ users, trips, cities, activities, expenses, packingItems, tripNotes, publicShares, savedDestinations }`. Do not hardcode counts; query the database.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - `apps/api/src/db/seedVerification.ts` contains `getSeedHealth`.
    - `apps/api/src/db/seedVerification.ts` contains `prisma.user.count`.
    - `apps/api/src/db/seedVerification.ts` contains `publicShares`.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Later health/status APIs can prove seed readiness through real database counts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Seed script to local database | Deterministic demo data is written into developer PostgreSQL. |
| Demo credentials to future auth flow | Seeded user data must not be mistaken for production credentials. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02 | Information Disclosure | demo user | mitigate | Use `.local` email and explicit placeholder password text until Phase 2 hashing is implemented. |
| T-03 | Information Disclosure | public share seed | mitigate | Seed a demo token and active state only for public demo itinerary data. |
| T-01 | Tampering | external data dependency | mitigate | Keep all seed values static and local; do not call travel APIs. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run db:seed`
- [ ] `npm run build`
- [ ] `rg -n "demo@traveloop.local|Spring Cities Circuit|demo-spring-cities|Seed complete" prisma/seed.ts`
</verification>

<success_criteria>
- FOUND-01 and FOUND-02 include deterministic demo data, not only empty tables.
- Seed data supports later city search, activity search, itinerary, budget, packing, notes, and sharing flows.
- Seed verification reads real database counts.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-database/01-03-SUMMARY.md`.
</output>
