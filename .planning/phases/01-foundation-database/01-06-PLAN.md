---
phase: 01-foundation-database
plan: 01-06
type: execute
wave: 4
depends_on:
  - "01-03"
  - "01-04"
  - "01-05"
files_modified:
  - package.json
  - README.md
  - apps/api/src/modules/health/health.routes.ts
  - apps/web/src/app/FoundationProbe.tsx
  - .planning/phases/01-foundation-database/01-SKELETON.md
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - FOUND-06
user_setup:
  - Run `docker compose up -d db` before full local verification.
must_haves:
  truths:
    - The full Phase 1 foundation can be verified with root commands and no external services beyond local PostgreSQL.
    - FOUND-01 FOUND-02 FOUND-03 FOUND-04 FOUND-05 FOUND-06 are each evidenced by source files and commands.
    - D-01 through D-18 are traceable from CONTEXT.md into implemented files and verification commands.
  artifacts:
    - `README.md` documents local setup, migration, seed, dev, and verification commands.
    - `package.json` contains root `verify` script that runs build and Prisma validation.
    - `01-SKELETON.md` has checked stack items after implementation.
  key_links:
    - `README.md` references `.env.example`, `docker compose up -d db`, `npm run db:migrate`, `npm run db:seed`, and `npm run dev`.
    - Health/status routes expose seed counts generated from Prisma queries.
---

<objective>
Tie the Phase 1 foundation together with verification and developer run documentation.

Purpose: Ensure the hackathon team can run, inspect, and prove the database-backed Traveloop skeleton before moving into traveler features.
Output: full-stack verification commands, health/status integration, README setup, and completed skeleton checklist.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-database/01-CONTEXT.md
@.planning/phases/01-foundation-database/01-RESEARCH.md
@.planning/phases/01-foundation-database/01-VALIDATION.md
@.planning/phases/01-foundation-database/01-SKELETON.md
@package.json
@apps/api/src/modules/health/health.routes.ts
@apps/web/src/app/FoundationProbe.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Connect health/status checks to real seed and schema state</name>
  <files>apps/api/src/modules/health/health.routes.ts, apps/web/src/app/FoundationProbe.tsx</files>
  <read_first>apps/api/src/modules/health/health.routes.ts, apps/api/src/db/seedVerification.ts, apps/web/src/app/FoundationProbe.tsx, prisma/schema.prisma</read_first>
  <action>Update health routes so `GET /api/health` returns `{ data: { status: "ok", database: "postgresql" } }`, `GET /api/status/seed` returns `{ data: { counts } }` using `getSeedHealth()`, and the foundation probe routes still create/read through Prisma. Update `FoundationProbe.tsx` to show health status and latest saved probe label after the button click, with user-facing text `Database-backed foundation is connected` when the API call succeeds. Keep errors visible with the UI-SPEC error text `We could not load your trip workspace. Check your connection and try again.`</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - `apps/api/src/modules/health/health.routes.ts` contains `database: "postgresql"`.
    - `apps/api/src/modules/health/health.routes.ts` contains `/status/seed`.
    - `apps/web/src/app/FoundationProbe.tsx` contains `Database-backed foundation is connected`.
    - `apps/web/src/app/FoundationProbe.tsx` contains `We could not load your trip workspace. Check your connection and try again.`
  </acceptance_criteria>
  <done>Health/status endpoints and the frontend probe demonstrate real database readiness.</done>
</task>

<task type="auto">
  <name>Task 2: Document and run the full Phase 1 verification path</name>
  <files>package.json, README.md, .planning/phases/01-foundation-database/01-SKELETON.md</files>
  <read_first>package.json, .env.example, docker-compose.yml, .planning/phases/01-foundation-database/01-VALIDATION.md, .planning/phases/01-foundation-database/01-SKELETON.md</read_first>
  <action>Ensure root `package.json` has script `"verify": "npm run build && npm run db:validate"`. Create `README.md` with sections `## Local setup`, `## Database`, `## Development`, `## Verification`, and `## Phase 1 scope guard`. The README must list commands exactly `cp .env.example .env`, `docker compose up -d db`, `npm install`, `npm run db:migrate`, `npm run db:seed`, `npm run dev`, `npm run verify`, and `npm run build`. Under scope guard, state that Phase 1 does not implement full auth, trip CRUD, itinerary builder, budget editing, packing editing, public sharing behavior, profile settings, notes editing, or admin analytics. Update `01-SKELETON.md` stack checklist items from unchecked `[ ]` to checked `[x]` only after the corresponding files and commands exist.</action>
  <verify>npm run verify</verify>
  <acceptance_criteria>
    - `README.md` contains `## Local setup`, `## Database`, `## Development`, `## Verification`, and `## Phase 1 scope guard`.
    - `README.md` contains `docker compose up -d db`, `npm run db:migrate`, `npm run db:seed`, and `npm run verify`.
    - `package.json` contains `"verify": "npm run build && npm run db:validate"`.
    - `.planning/phases/01-foundation-database/01-SKELETON.md` contains `[x] Project scaffold`, `[x] Routing`, `[x] Database`, `[x] UI`, and `[x] Deployment`.
    - `npm run verify` exits 0.
  </acceptance_criteria>
  <done>Developers and judges have a clear local proof path for the Phase 1 foundation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local developer to database | README commands guide local schema and seed changes. |
| API health to frontend status | Health/status data is shown in the web shell. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01 | Tampering | verification scripts | mitigate | Root `verify` must run build and Prisma validation. |
| T-02 | Information Disclosure | README setup | mitigate | Document `.env.example` copying without committing `.env`. |
| T-05 | Repudiation | phase evidence | mitigate | README and skeleton checklist record exact commands and scope boundaries. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run db:migrate`
- [ ] `npm run db:seed`
- [ ] `npm run verify`
- [ ] `rg -n "docker compose up -d db|npm run db:migrate|npm run db:seed|npm run verify" README.md`
- [ ] `rg -n "\\[x\\] Project scaffold|\\[x\\] Database|\\[x\\] UI" .planning/phases/01-foundation-database/01-SKELETON.md`
</verification>

<success_criteria>
- All Phase 1 requirements are covered by a command-backed verification path.
- The Walking Skeleton contract is updated with evidence after implementation.
- The local setup remains PostgreSQL-only and hackathon-scope compliant.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-database/01-06-SUMMARY.md`.
</output>
