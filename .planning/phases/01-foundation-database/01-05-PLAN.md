---
phase: 01-foundation-database
plan: 01-05
type: execute
wave: 3
depends_on:
  - "01-01"
files_modified:
  - apps/web/src/routes/routeInventory.ts
  - apps/web/src/styles/tokens.css
  - apps/web/src/styles/global.css
  - apps/web/src/components/AppShell.tsx
  - apps/web/src/components/PageScaffold.tsx
  - apps/web/src/components/SurfaceCard.tsx
  - apps/web/src/App.tsx
autonomous: true
requirements:
  - FOUND-05
  - FOUND-06
user_setup: []
must_haves:
  truths:
    - D-16 D-17 D-18 are implemented as a supplied-only route inventory, basic responsive shell, and Traveloop brand tokens.
    - The frontend shell does not implement full later page layouts or invented routes.
    - UI primitives follow the approved Phase 1 UI-SPEC colors, type scale, spacing, radius, and no nested-card rule.
  artifacts:
    - `apps/web/src/routes/routeInventory.ts` lists only supplied Traveloop routes and references source image filenames.
    - `apps/web/src/styles/tokens.css` defines `#F5F1EA`, `#FFFFFF`, `#5E6B52`, `#1F231E`, `#6E746A`, and `#E7E2D7`.
    - `apps/web/src/components/AppShell.tsx` and `PageScaffold.tsx` provide reusable shell primitives.
  key_links:
    - `apps/web/src/App.tsx` renders `AppShell` and route inventory placeholders.
    - `routeInventory.ts` maps route labels to supplied visual page source filenames.
---

<objective>
Create the frontend route inventory and Traveloop visual foundation.

Purpose: Give later UI phases a constrained, brand-consistent shell without inventing screens or building full pages early.
Output: route inventory constants, global CSS tokens, and reusable shell primitives.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-database/01-CONTEXT.md
@.planning/phases/01-foundation-database/01-UI-SPEC.md
@apps/web/src/App.tsx
@apps/web/src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Encode supplied-only route inventory</name>
  <files>apps/web/src/routes/routeInventory.ts</files>
  <read_first>.planning/phases/01-foundation-database/01-UI-SPEC.md, .planning/ROADMAP.md, hackathon-inputs/wireframes/Untitled-2026-05-10-0855.svg</read_first>
  <action>Create `routeInventory.ts` exporting `traveloopRoutes` with route entries for login/signup, registration, dashboard/home, create trip, my trips, itinerary builder, itinerary view, city/activity search, shared/community itinerary, packing checklist, expense/budget detail, trip notes/journal, user profile/settings, and optional admin analytics marked `deferred: true`. Each entry must include `id`, `path`, `label`, `phase`, `source`, and `deferred`. Use paths exactly `/login`, `/register`, `/dashboard`, `/trips/new`, `/trips`, `/trips/:tripId/build`, `/trips/:tripId/itinerary`, `/search`, `/share/:token`, `/trips/:tripId/packing`, `/trips/:tripId/budget`, `/trips/:tripId/notes`, `/profile`, and `/admin`. The `source` values must include the supplied filenames from `01-UI-SPEC.md`, including `03-Main-Landing-Page.png`, `04-Create-a-new-Trip.png`, and `14-Trip notes or journal screen .png`. Do not add booking, payment, maps, blog, AI planner, or marketing routes.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - `apps/web/src/routes/routeInventory.ts` contains `/dashboard`, `/trips/new`, `/trips/:tripId/itinerary`, `/search`, `/share/:token`, and `/profile`.
    - `apps/web/src/routes/routeInventory.ts` contains `deferred: true` for `/admin`.
    - `apps/web/src/routes/routeInventory.ts` contains `03-Main-Landing-Page.png` and `14-Trip notes or journal screen .png`.
    - `rg -n "booking|payment|maps|blog|AI planner|marketing" apps/web/src/routes/routeInventory.ts` returns no matches.
  </acceptance_criteria>
  <done>The frontend knows only the approved Traveloop route surface.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Traveloop tokens and shell primitives</name>
  <files>apps/web/src/styles/tokens.css, apps/web/src/styles/global.css, apps/web/src/components/AppShell.tsx, apps/web/src/components/PageScaffold.tsx, apps/web/src/components/SurfaceCard.tsx, apps/web/src/App.tsx</files>
  <read_first>apps/web/src/App.tsx, apps/web/src/styles/global.css, .planning/phases/01-foundation-database/01-UI-SPEC.md, hackathon-inputs/ui-brand-dna/brand-dna-1.png</read_first>
  <action>Create `tokens.css` with CSS variables exactly `--color-page: #F5F1EA`, `--color-surface: #FFFFFF`, `--color-accent: #5E6B52`, `--color-ink: #1F231E`, `--color-muted: #6E746A`, `--color-border: #E7E2D7`, `--color-soft-olive: #A8B38E`, `--color-warm-clay: #B2765C`, spacing `--space-xs: 4px` through `--space-3xl: 64px`, `--radius-card: 8px`, and font stacks preferring `"Playfair Display"` for headings and `"Inter"` for body. Import `tokens.css` from `global.css`. Add global body background, text color, `letter-spacing: 0`, responsive gutters, visible focus outline using `#5E6B52`, and no viewport-scaled font sizes. Create `AppShell`, `PageScaffold`, and `SurfaceCard` primitives using 8px card radius and no nested cards. Update `App.tsx` to render the Traveloop wordmark, compact nav from `traveloopRoutes`, the `FoundationProbe` component from plan 01, and route placeholder cards labelled from the inventory.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - `apps/web/src/styles/tokens.css` contains `--color-page: #F5F1EA`, `--color-accent: #5E6B52`, and `--radius-card: 8px`.
    - `apps/web/src/styles/tokens.css` contains `Playfair Display` and `Inter`.
    - `apps/web/src/components/AppShell.tsx` contains `Traveloop`.
    - `apps/web/src/App.tsx` imports `traveloopRoutes`.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>The shell has global visual primitives consistent with the approved UI design contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Supplied inputs to route inventory | Source filenames become route metadata for future implementation. |
| API probe state to frontend shell | The foundation probe displays API data in the web shell. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01 | Tampering | route inventory | mitigate | Keep route inventory in typed constants and limited to supplied sources. |
| T-01-05 | Spoofing | visual shell | mitigate | Use a consistent Traveloop wordmark and route labels; avoid unrelated screens. |
| T-05 | Tampering | frontend API input | mitigate | Keep probe input bounded and rely on API Zod validation. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run build`
- [ ] `rg -n "#F5F1EA|#5E6B52|Playfair Display|Inter" apps/web/src/styles`
- [ ] `rg -n "/dashboard|/trips/new|/share/:token|deferred: true" apps/web/src/routes/routeInventory.ts`
</verification>

<success_criteria>
- FOUND-05 and FOUND-06 are implemented as durable frontend foundations.
- The route inventory contains approved supplied screens only.
- The shell feels like Traveloop but does not build full Phase 2-5 pages early.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-database/01-05-SUMMARY.md`.
</output>
